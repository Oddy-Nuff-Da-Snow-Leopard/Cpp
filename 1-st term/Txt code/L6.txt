7 Вариант 


#include <iostream>
#include <conio.h>
using namespace std;
void main()
{
	setlocale (LC_CTYPE, "Russian");
	double  m , w, v, a = 1.2, x = 2.5, k = 3;
	for (m = 4; m <= 6.1;m+=0.2) 
	{
		if (x < (m / 2))
			w = sqrt(0.2*x)*k;
		else
			w = 1*exp(2*x*k);
		v = sqrt(pow(w, 3) + abs(x - a)) / log(1 + a);
		cout << "Значение m = " << m << endl;
		cout << "Значение w = " << w << endl;
		cout << "Значение v = " << v << endl << "---------------------------" << endl;
	} 
	getch();
}
==========================================================================================================
6.Используя перебор значений вывести на экран в убывающем порядке все
двузначные числа, в десятичной записи которых нет одинаковых цифр.

#include <iostream>
#include <conio.h>
using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");
	int a, b;
	for (a = 9; a >= 1; a--)
		for (b = 9; b >= 0; b--)
			if (a != b)
				cout << a * 10 + b << ' ';
	getch();
}
=============================================================================================================
2.Найти сумму целых положительных чисел, кратных 3 и меньших 200.

#include <iostream>
#include <conio.h>
using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");
	int a=1,s=0;
	while (a<=200)
	{
		if (a % 3 == 0)
			s += a;
		a++;
	}
	cout << s;
    getch();
}
=============================================================================================================
3.Определить количество трехзначных натуральных чисел, сумма цифр которых равна n.

using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");
	int a, b, c, n, p=0;
	cout << "Введите число от 1 до 27 :  ";
	cin >> n;
	for (a = 1; a <= 9; a++)
		for (b = 0; b <= 9; b++)
			for (c = 0; c <= 9; c++)
				if ((a + b + c) == n)
					
				{
					cout << a * 100 + b * 10 + c << ' ';
					p++;
				}
	
	cout << endl << "Количество чисел,сумма цифр которых равна заданному числу : " << p << endl;
    getch();
}
=============================================================================================================
7.Даны натуральные числа m, n.
Используя перебор значений, получить все меньшие n натуральные числа, квадрат суммы цифр которых равен m.

#include <iostream>
#include <conio.h>
using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");
	int m, n, a, b, c;
	cout << "Вводимые числа должны содержать не более 3-ёх цифр " << endl;
	cout << "Введите число m  :  ";
	cin >> m;
	cout << "Введите число n  :  ";
	cin >> n;
	for (a = 1; a <= 9; a++)
		for (b = 0; b <= 9; b++)
			for (c = 0; c <= 9; c++)
			if (pow(a + c +b, 2) == m & a * 100 + b*10 + c < n)
				cout << a * 100 + b*10 + c << ' ';
	getch();
}
=============================================================================================================
Дополнительные задания


3.Имеются два сосуда. В первом сосуде находится C1 литров воды, во втором – C2 литров воды.
Из первого сосуда переливают половину воды во второй сосуд, затем из второго переливают половину в первый сосуд, и т. д. 
Сколько воды окажется в обоих сосудах после 12 переливаний?

#include <iostream>
#include <conio.h>
using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");
	int n;
	float c1, c2;
	cout << "Введите сколько литров первоначально в первом сосуде  :  ";
	cin >> c1;
	cout << "Введите сколько литров первоначально во втором сосуде  :  ";
	cin >> c2;
	cout << "Введите количество переливаний  :  ";
	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		c1 = c1 / 2;
		c2 = c2 + c1;
		c2 = c2 / 2;
		c1 = c1 + c2;
	}
	cout << c1 << endl << c2 << endl;
	getch();
}