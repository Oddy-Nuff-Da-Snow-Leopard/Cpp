Написать и выполнить программу по первой блок-схеме лабораторной работы № 1.
#include <iostream>
#include <conio.h>
using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");

	double a, b, c, D, x1, x2, x;
	cout << "Квадратное уравнение имеет вид ax^2 + bx +c = 0 ,введите коэффициенты a, b и c " << endl;
	cout << "a = ";
	cin >> a;
	cout << "b = ";
	cin >> b;
	cout << "c = ";
	cin >> c;
	D = pow(b, 2) - 4 * a*c;
	if (D > 0) {
		x1 = (-b + sqrt(D)) / (a * 2);
		x2 = (-b - sqrt(D)) / (a * 2);
		cout << "Дискриминант больше нуля,значит,существуют два корня квадратного уравнения " << endl;
		cout << x1 << endl << x2 << endl;
	}
	else if (D == 0) {
		x = -b / (2 * a);
		cout << "Дискриминант равен нулю,значит,существует единственный корень квадратного уравнения " << endl;
		cout << x << endl;
	}
	else
		cout << "Дискриминант меньше нуля,значит,корни квадратного уравнения не существуют " << endl;
	cout << endl << endl << endl;
===============
В переменную Year ввести номер года. Определить, является ли год високосным.
	int year;
	/*Год является високосным,если он делится без остатнка на 4,но не будет им являться,если делится
	без остатка на 100.Также год будет висоскосным,если он делится на 400 без остатка.*/
	cout << "Введите год : ";
	cin >> year;
	if (year % 4 == 0 & year % 100 != 0 | year % 400 == 0)
		cout << "Год висоскосный " << endl;
	else
		cout << "Год не високосный " << endl;
	cout << endl << endl << endl;
===============
Диалог на любую тему.
	int c1, c2, c3;
	cout << "Выберите страну,в которую хотели бы поехать отдыхать \n   1-Россия 2-Украина " << endl;
	cin >> c1;
	switch (c1) {
	case 1: cout << "Выберите регион \n   1-Крым 2-Чечня " << endl;
		cin >> c2; 
		switch (c2) {
		case 1: cout << "ЕЕЕЕЕЕ_КРЫМ НАШ!!!11!" << endl; break;
		case 2: cout << "Чечня это круто!1!111!" << endl; break;
		default: cout << "Проверьте правильность выбора " << endl;
		}
		break;
	case 2: cout << "Выберите регион \n 1-ДНР 2-ЛНР" << endl;
		cin >> c3;
		switch(c3){
		case 1: cout << "ЧЁ ПАЦАНЫ НА ДОНБАСС?" << endl; break;
		case 2: cout << "Украина ЦЭ ЕВРОПА!1!!!" << endl; break;
		default: cout << "Проверьте правильность выбора " << endl;
		}
		break;
	default: cout << "Проверьте правильность выбора " << endl;
	}
	_getch();
}